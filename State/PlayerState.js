class PlayerState {
    constructor() {
        //flags for certain points in a story-ish setting such that when talking to diff ppl it might unlock something new etc
        this.storyFlags = {
            //"did smth" : true,
            
        };
    }
}

window.playerState = new PlayerState();